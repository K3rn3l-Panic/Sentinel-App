const home = require('./home-controller');
const users = require('./users-controller');
const camera = require('./camera-controller');
const faces = require('./faces-controller');
const alerts = require('./alerts-controller');

module.exports = {
  home,
  users,
  camera,
  faces,
  alerts,
};
